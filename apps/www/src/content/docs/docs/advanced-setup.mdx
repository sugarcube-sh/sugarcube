---
title: Advanced Setup
description: Customize directory paths during init.
---

## Custom directories

By default, `init` places tokens in `src/design-tokens` (or `design-tokens` if there's no `src/` directory), and CUBE CSS in `src/styles`.

You can override both:

<PackageManagerTabs command="npx @sugarcube-sh/cli init --tokens-dir ./tokens --cube-dir ./css" />
