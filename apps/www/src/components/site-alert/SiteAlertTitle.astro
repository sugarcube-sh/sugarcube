---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"div"> {}

const { class: className, ...props } = Astro.props;
---

<div data-slot="site-alert-title" class:list={["site-alert-title", className]} {...props}>
    <slot />
</div>
