---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"div"> {}

const { class: className, ...props } = Astro.props;
---

<div
    data-slot="site-alert-description"
    class:list={["site-alert-description", className]}
    {...props}
>
    <slot />
</div>
