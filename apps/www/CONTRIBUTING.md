# Contributing to apps/www

## Run the site

```bash
pnpm dev
pnpm dev:debug      # for token-pipeline logs
```

## A note on the devframes hub

In dev, a devframes hub is mounted, so Studio appears as a dock on every
page and on its own at `/__devframes/sugarcube-studio/`.
