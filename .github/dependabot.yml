version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    cooldown:
      default-days: 7
    open-pull-requests-limit: 10
    groups:
      production-dependencies:
        dependency-type: "production"
        update-types:
          - minor
          - patch
      development-dependencies:
        dependency-type: "development"
        update-types:
          - minor
          - patch
        # I want to keep these updates reviewable on their own for now.
        exclude-patterns:
          - "@vitejs/devtools*"

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      github-actions:
        patterns:
          - "*"
